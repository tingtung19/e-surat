@extends('layouts.app')
@section('content')
<div class="page-heading"><div><h1>Kategori Surat</h1><p>Kelola kategori dan sub-kategori hingga dua level.</p><div class="breadcrumb"><a href="{{ route('dashboard') }}">Beranda</a> &nbsp;/&nbsp; Kategori Surat</div></div><a class="btn" href="{{ route('master.categories.create') }}"><ion-icon name="add-outline"></ion-icon> Tambah kategori</a></div>
<div class="card content-card"><div class="card-header"><h2>Daftar kategori</h2></div><div class="table-wrap"><table><thead><tr><th>Nama</th><th>Parent</th><th>Format nomor</th><th>Sub-kategori</th><th>Aksi</th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->name }}</strong></td><td>{{ $category->parent?->name ?? 'Kategori induk' }}</td><td>{{ $category->number_format ?? '-' }}</td><td>{{ $category->children_count }}</td><td><a class="btn secondary" href="{{ route('master.categories.edit',$category) }}">Edit</a> <form style="display:inline" method="post" action="{{ route('master.categories.destroy',$category) }}" onsubmit="return confirm('Hapus kategori ini?')">@csrf @method('DELETE')<button class="btn secondary">Hapus</button></form></td></tr>@empty<tr><td colspan="5">Belum ada kategori.</td></tr>@endforelse</tbody></table></div>{{ $categories->links() }}</div>
@endsection
