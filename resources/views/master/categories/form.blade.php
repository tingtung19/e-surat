<div class="form-grid"><div class="form-group"><label>Nama kategori *</label><input name="name" value="{{ old('name', $category->name ?? '') }}" required></div><div class="form-group"><label>Parent kategori</label><select name="parent_id"><option value="">Kategori induk</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected(old('parent_id', $category->parent_id ?? '') == $parent->id)>{{ $parent->name }}</option>@endforeach</select><small class="form-help">Kosongkan untuk membuat kategori induk.</small></div><div class="form-group"><label>Format nomor surat</label><input name="number_format" value="{{ old('number_format', $category->number_format ?? '') }}" placeholder="Contoh: UM/{year}/{number}"></div><div class="form-group"><label>Keterangan</label><input name="description" value="{{ old('description', $category->description ?? '') }}"></div></div>
